#include <stdio.h>

int main(void)
{
    float CONSTANT = 3.14; //Constant
    int variable = 10; //Variable
    float expression = (2 + 3) * 10;
}

int add(int x, int y)
{
    int result;
    result = x + y;
    return (result);
}
